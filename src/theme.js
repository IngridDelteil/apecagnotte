import {createTheme} from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: "Cookie",
  },
  palette: {
    primary: {
      main: "#0c4a6e",
    },
    secondary: {
      main: "#14b8a6",
    },
    info: {
      main: "#fb7185",
    },
  },
});

export default theme;
